# 이 파일을 복사해서 .env로 저장하고, 아래 값을 서울 열린데이터광장에서 발급받은
# 실제 인증키로 바꿔주세요. .env는 절대 GitHub에 올리면 안 됩니다 (.gitignore에 이미 등록됨).
#
# Vercel에 배포할 때는 이 파일이 아니라, Vercel 프로젝트의
# Settings > Environment Variables 화면에 같은 이름(SEOUL_API_KEY)으로 등록합니다.
SEOUL_API_KEY=여기에_인증키_입력
